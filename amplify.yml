version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd viewer
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: viewer/out
    files:
      - '**/*'
  cache:
    paths:
      - viewer/node_modules/**/*
